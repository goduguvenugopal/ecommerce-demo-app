import { StyleSheet } from 'react-native';

const styles: any = StyleSheet.create({
  header: {
    fontSize: 28,
    fontWeight: 'bold',
    color: '#2874F0',
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    zIndex: 10,
  },
});

export default styles;
